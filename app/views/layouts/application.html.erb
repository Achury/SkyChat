<!DOCTYPE html>
<html lang = 'en'>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Chatte Web" %></title>
    <%= stylesheet_link_tag "application", :media => "all"%>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
    <div id="container">
      <div id="header">
        <% if logged_in? %>
          You are <%= current_nickname %>. <%= link_to "Log out", session_path, :method => :delete %>.
        <% end %>
      </div>

      <div class="center"><h1>Sky Chat</h1></div>

      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
